const MIN_LENGTH = 8;
const MAX_LENGTH = 50;
const VALID_CHARS = /^[a-zA-Z0-9-_]+$/;
const ALL_NUMS = /^\d+$/;

export const MessageConstants = Object.freeze({
  MIN_LENGTH,
  MAX_LENGTH,
  VALID_CHARS,
  ALL_NUMS,
});
